#module is lines of python code and saved in a .py file and the variables and methods can be invoked in another program

x = 90
def sum(a,b):
    result = 'Sum of {} & {} is : {}'
    print(result.format(a,b,a+b))
